# Backend API base URL. Vite only exposes vars prefixed with VITE_ to client code.
# Local dev default (used automatically if this var is unset): http://127.0.0.1:8000
VITE_API_BASE_URL=https://veryon-excel-transformation-backend.vercel.app
